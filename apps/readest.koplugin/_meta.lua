local _ = require("gettext")
return {
    name = "readest",
    fullname = _("Readest sync"),
    description = _([[Sync your reading progress, highlights, and books with Readest cloud storage.

Features:
• Sync reading progress and highlights
• Upload and download books to/from cloud storage
• Sync entire library metadata
• Offline queue for automatic retry
• Storage statistics and file management
]]),
}
